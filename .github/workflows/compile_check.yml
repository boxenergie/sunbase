name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Typescript compiler
      uses: iCrawl/action-tsc@v1
      with:
        name: Sunbase
